import { Card } from '@mui/material'
import { FC } from 'react'

const Details: FC = () => {
	return <Card sx={{ p: 1, height: '100%', overflowY: 'auto', scrollbarWidth: 'thin' }}></Card>
}

export default Details
